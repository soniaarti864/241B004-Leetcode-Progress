class Solution(object):
    def permute(self, nums):
        perms=[[]]
        for num in nums:
            new_perms=[]
            for p in perms:
                for i in range(len(p)+1):
                    new_perms.append(p[:i]+[num]+p[i:])
            perms=new_perms
        return perms
        
